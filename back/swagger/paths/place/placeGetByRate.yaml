get:
  tags:
    - place
  summary: 별점순으로 불러오기
  description: Get 방식으로 별점순으로 장소들을 가져옵니다. Main 페이지에서 쓰일 예정으로 card를 rendering하는데 필요한 정보들만 가져옵니다. MainSelect 부분에서 쓰면 될 것 같습니다.
  responses:
    200:
      description: 장소 가져오기 성공.
    400:
      description: 장소 가져오기 실패.
