import styled from 'styled-components';

import { Colors } from '@styles';

export const FullBarWrapper = styled.div`
  height: 2px;
  width: 100%;
  border-radius: 0px;
  background-color: ${Colors.lightGrey};
`;
