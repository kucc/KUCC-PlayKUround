import styled from 'styled-components';

export const IconWrapper = styled.div`
  position: relative;
  left: 53px;
  bottom: 14px;
  svg:first-child {
    position: relative;
  }
  svg:last-child {
    position: absolute;
    left: 9.5px;
    bottom: 22px;
  }
  cursor: pointer;
`;
